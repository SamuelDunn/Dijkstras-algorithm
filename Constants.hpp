# pragma once

/*
This file contains constants used in Dijkstra's algorithm. Each node in the
graph starts at an distance of infinity from the source node. Infinity is
represented using the max integer.
*/

#include <limits>

const int INF = std::numeric_limits<int>::max();

